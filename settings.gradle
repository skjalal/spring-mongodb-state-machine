pluginManagement {
    plugins {
        id 'org.springframework.boot' version SPRING_BOOT_VERSION
        id 'io.spring.dependency-management' version SPRING_IO_VERSION
    }
}
rootProject.name = 'spring-mongodb-state-machine'
